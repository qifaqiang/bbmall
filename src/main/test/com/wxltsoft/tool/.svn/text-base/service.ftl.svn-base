package ${PACKAGE_URL};

import java.util.List;
import ${PACKAGE};

/**
 * @文件名称: ${CLASS}Service.java
 * @类路径: ${CLASSPATH}
 * @描述: TODO
 * @作者：${AUTH_NAME}
 * @公司：${COMPANY_NAME}
 * @时间：${TIME}
 */
public interface  ${CLASS}Service {
	// 根据主键删除
	int deleteByPrimaryKey(Integer id);

	// 根据所需插入
	int insertSelective(${CLASS} record);

	// 根据所需更新
	int updateByPrimaryKeySelective(${CLASS} record);

	// 根据主键查询
	${CLASS} selectByPrimaryKey(Integer id);

	// 根据所需查询
	${CLASS} selectBy${CLASS}(${CLASS} record);

	// 分页获取
	List<${CLASS}> listPageBy${CLASS}(${CLASS} clssname);

	// 获取全部
	List<${CLASS}> getAllBy${CLASS}(${CLASS} clssname);
}